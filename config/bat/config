# Force colorizing output :
--color=always

# highlighting theme
--theme="Kanagawa"

# Enable this to use italic text on the terminal. This is not supported on all terminal emulators (like tmux, by default):
#--italic-text=always

# disable automatic paging
--paging=never

# Enable mouse scrolling support in `bat` when running inside tmux. This might disable text selection, unless you press shift.
--pager="less --RAW-CONTROL-CHARS --quit-if-one-screen --mouse"

# Syntax mappings: map a certain filename pattern to a language.
--map-syntax "*.rasi:CSS"
--map-syntax "*zsh*:Bourne Again Shell (bash)"
--map-syntax ".zcompdump:Bourne Again Shell (bash)"
--map-syntax "config:Bourne Again Shell (bash)"
--map-syntax "*.conf:Bourne Again Shell (bash)"
